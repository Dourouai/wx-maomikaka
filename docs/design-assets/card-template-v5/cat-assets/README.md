# L3 猫咪主体输入

这里放已经由 AI 单独生成并完成透明抠图的猫 PNG。

要求：RGBA、无背景、无地面、无文字或水印；保留耳朵、胡须、爪子、尾巴和毛发边缘。文件名使用 `cat-{catAssetId}.png`，并在卡片数据中记录 `anchorX`、`baseY`、`scale`。

当前工作区尚未放入真实的独立透明猫 PNG。收到后直接作为 L3 使用，不需要修改相框和合成坐标。
